import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    marginTop: 10,
  },
  headText: {
    fontSize: 22,
    fontWeight: "600",
    marginBottom: 10,
  },
  contextText: {
    fontSize: 17,
  },
});
