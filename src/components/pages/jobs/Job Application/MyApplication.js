import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MyApplication = () => {
  return (
    <View>
      <Text>MyApplication</Text>
    </View>
  );
};

export default MyApplication;

const styles = StyleSheet.create({});
