import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SavedJobsCard = () => {
  return (
    <View>
      <Text>SavedJobsCard</Text>
    </View>
  );
};

export default SavedJobsCard;

const styles = StyleSheet.create({});
